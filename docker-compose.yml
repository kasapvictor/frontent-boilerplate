version: '3'

services:
  web:
    build: .

    image: ${IMAGE_NAME}

    container_name: ${CONTAINER_NAME}

    restart: always

    volumes:
      - "./:/home/app"

    ports:
      - ${PORT}:${PORT}

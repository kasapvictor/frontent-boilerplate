version: '3'

services:
  frontend:
    platform: linux/arm64
    container_name: frontend
    restart: always
    image: node:18
    working_dir: /app
    volumes:
      - "./:/app"
    ports:
      - "4444:4444"
    command: sh -c "npm install -g npm@8.11.0 && npm install && npm run server"
